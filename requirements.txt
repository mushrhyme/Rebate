# Streamlit
streamlit>=1.28.0

# 데이터 처리
pandas>=2.0.0
openpyxl>=3.1.0  # Excel 파일 생성용

# 이미지 처리
Pillow>=10.0.0

# PDF 처리
PyMuPDF>=1.23.0
pdfplumber>=0.10.0  # PDF 테이블 추출용 (엑셀 변환)
tabula-py>=2.8.0  # PDF 테이블 추출용 (선택적, Java 필요)

# Gemini API
google-generativeai>=0.3.0

# 환경 변수 관리
python-dotenv>=1.0.0

# AG Grid (선택적 - 없으면 기본 data_editor 사용)
streamlit-aggrid>=0.3.4

# 데이터베이스 (PostgreSQL - 선택적)
psycopg2-binary>=2.9.0

# 벡터 DB (RAG용) - FAISS 사용
faiss-cpu>=1.7.0

# RAG 검색 개선 (선택적)
rank-bm25>=0.2.2  # BM25 하이브리드 검색용
sentence-transformers==2.2.2  # 메타 텐서 문제 방지를 위해 버전 고정
transformers==4.35.0  # device_map 사용을 피하기 위한 안정 버전
torch>=2.0.0,<2.2.0  # PyTorch 안정 버전 범위
numpy<2.0.0  # NumPy 2.x 호환성 문제 방지

