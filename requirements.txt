# Streamlit
streamlit>=1.28.0

# 데이터 처리
pandas>=2.0.0
openpyxl>=3.1.0  # Excel 파일 생성용

# 이미지 처리
Pillow>=10.0.0

# PDF 처리
PyMuPDF>=1.23.0

# Gemini API
google-generativeai>=0.3.0

# 환경 변수 관리
python-dotenv>=1.0.0

# AG Grid (선택적 - 없으면 기본 data_editor 사용)
streamlit-aggrid>=0.3.4

# 데이터베이스 (PostgreSQL - 선택적)
psycopg2-binary>=2.9.0

# 벡터 DB (RAG용) - FAISS 사용
faiss-cpu>=1.7.0

# RAG 검색 개선 (선택적)
rank-bm25>=0.2.2  # BM25 하이브리드 검색용
sentence-transformers>=2.2.0  # 임베딩 모델 및 Re-ranking용

