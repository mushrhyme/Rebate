# 📐 프로젝트 구조 개요도

## 🏗️ 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                         app.py (메인 진입점)                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Streamlit 웹앱 (3개 탭)                                  │  │
│  │  • 📤 업로드 & 분석 탭                                     │  │
│  │  • 📝 검토 탭                                             │  │
│  │  • 📥 다운로드 탭                                         │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ├─────────────────────────────────────┐
                              │                                     │
                              ▼                                     ▼
        ┌──────────────────────────────┐         ┌──────────────────────────────┐
        │   modules/core/              │         │   modules/ui/                │
        │   (핵심 비즈니스 로직)        │         │   (UI 컴포넌트)              │
        │                              │         │                              │
        │  • PdfProcessor              │         │  • review_components        │
        │    - process_pdf()            │         │    - render_navigation()     │
        │    - process_uploaded_pdf()   │         │    - render_page_image()     │
        │                              │         │    - render_editable_table()│
        │  • PdfRegistry               │         │  • aggrid_utils             │
        │    - 분석 대기열 관리          │         │    - AgGrid 테이블 유틸      │
        │                              │         │                              │
        │  • PageStorage               │         │                              │
        │    - 페이지 결과 저장/로드     │         │                              │
        └──────────────────────────────┘         └──────────────────────────────┘
                              │                                     │
                              └──────────────┬──────────────────────┘
                                             │
                                             ▼
        ┌──────────────────────────────────────────────────────────┐
        │              parser/ (파싱 로직)                          │
        │  • VisionParser                                          │
        │    - parse_pdf()                                         │
        │    - PDF → 이미지 변환                                    │
        │                                                           │
        │  • MergeUtils                                            │
        │    - merge_all_pages()                                   │
        │    - 페이지별 JSON → DataFrame 병합                       │
        └──────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌──────────────────────────────────────────────────────────┐
        │              src/ (Gemini API 통합)                       │
        │  • GeminiVisionParser                                    │
        │    - parse_image()                                       │
        │    - Gemini Vision API 호출                               │
        │    - 멀티스레딩 지원 (최대 5개 스레드)                     │
        └──────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌──────────────────────────────────────────────────────────┐
        │              database/ (PostgreSQL)                      │
        │  • DatabaseManager                                       │
        │    - save_from_page_results()                            │
        │    - get_page_results()                                  │
        │    - has_pdf_in_db()                                     │
        │                                                           │
        │  테이블 구조:                                             │
        │  • parsing_sessions (세션 관리)                           │
        │  • items (파싱 결과 데이터)                                │
        └──────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌──────────────────────────────────────────────────────────┐
        │              utils/ (유틸리티)                            │
        │  • SessionManager                                        │
        │    - 세션별 파일 관리                                     │
        │    - PDF/이미지/썸네일 저장                               │
        │    - OCR 결과 로드 (DB 우선, 파일 시스템 폴백)            │
        └──────────────────────────────────────────────────────────┘
```

## 📁 디렉토리 구조

```
Rebate/
├── app.py                          # 메인 Streamlit 앱
├── batch_parse_pdfs.py            # 배치 처리 스크립트
├── pdf_registry.json              # 분석 대기열 관리
│
├── modules/                        # 모듈화된 기능
│   ├── core/                      # 핵심 비즈니스 로직
│   │   ├── processor.py           # PdfProcessor (PDF 처리)
│   │   ├── registry.py            # PdfRegistry (대기열 관리)
│   │   └── storage.py             # PageStorage (결과 저장)
│   │
│   ├── ui/                        # UI 컴포넌트
│   │   ├── review_components.py  # 검토 탭 컴포넌트
│   │   └── aggrid_utils.py       # AgGrid 유틸리티
│   │
│   └── utils/                     # 유틸리티
│       └── pdf_utils.py          # PDF 관련 유틸
│
├── parser/                        # 파싱 로직
│   ├── vision_parser.py          # VisionParser (PDF → 이미지)
│   └── merge_utils.py            # MergeUtils (JSON 병합)
│
├── src/                           # 외부 API 통합
│   └── gemini_extractor.py       # GeminiVisionParser
│
├── database/                      # 데이터베이스
│   ├── db_manager.py             # DatabaseManager
│   ├── schema.sql                # DB 스키마
│   ├── reset_schema.py           # 스키마 리셋
│   └── drop_all_tables.py        # 테이블 삭제
│
├── utils/                         # 세션 관리
│   └── session_manager.py        # SessionManager
│
├── img/                           # 이미지 저장 (임시)
├── result/                        # JSON 결과 저장 (레거시)
└── requirements.txt               # 의존성
```

## 🔄 데이터 흐름

### 1. 업로드 & 분석 흐름

```
사용자 업로드
    │
    ▼
app.py (render_upload_tab)
    │
    ├─→ check_pdf_in_db()          # DB 존재 여부 확인
    │
    ├─→ process_pdf_with_progress()
    │   │
    │   ├─→ PdfProcessor.process_uploaded_pdf()
    │   │   │
    │   │   ├─→ SessionManager.save_pdf_file()  # 파일 저장
    │   │   │
    │   │   └─→ PdfProcessor.process_pdf()
    │   │       │
    │   │       ├─→ VisionParser.parse_pdf()    # PDF → 이미지
    │   │       │
    │   │       └─→ GeminiVisionParser.parse_image()  # 이미지 → JSON
    │   │           │
    │   │           └─→ ThreadPoolExecutor (멀티스레딩)
    │   │
    │   └─→ DatabaseManager.save_from_page_results()  # DB 저장
    │
    └─→ session_state 업데이트
```

### 2. 검토 탭 흐름

```
app.py (render_review_tab)
    │
    ├─→ review_components.render_navigation()
    │
    ├─→ review_components.load_page_data()
    │   │
    │   └─→ SessionManager.load_ocr_result()
    │       │
    │       ├─→ DatabaseManager.get_page_result()  # DB 우선
    │       │
    │       └─→ PageStorage.load_page()            # 폴백
    │
    ├─→ review_components.render_page_image()
    │
    └─→ review_components.render_editable_table()
        │
        └─→ AgGridUtils (선택적)
```

### 3. 다운로드 탭 흐름

```
app.py (render_download_tab)
    │
    ├─→ SessionManager.load_ocr_result()  # 각 페이지 로드
    │
    ├─→ MergeUtils.merge_all_pages()     # JSON 병합
    │
    └─→ pandas DataFrame → Excel          # 엑셀 다운로드
```

## 🧩 주요 클래스 관계

```
┌─────────────────┐
│   app.py        │
│  (Streamlit UI) │
└────────┬────────┘
         │
         ├─────────────────────────────────────────────┐
         │                                             │
         ▼                                             ▼
┌──────────────────┐                      ┌──────────────────┐
│  PdfProcessor    │                      │ SessionManager   │
│  (처리 로직)      │                      │ (파일 관리)       │
└────────┬─────────┘                      └────────┬─────────┘
         │                                          │
         ├──────────────┐                          │
         │              │                          │
         ▼              ▼                          ▼
┌─────────────┐  ┌─────────────┐         ┌─────────────┐
│ VisionParser│  │PdfRegistry  │         │PageStorage  │
│             │  │(대기열)      │         │(결과 저장)   │
└──────┬──────┘  └─────────────┘         └─────────────┘
       │
       ▼
┌──────────────────┐
│ GeminiVisionParser│
│  (Gemini API)     │
└───────────────────┘
         │
         ▼
┌──────────────────┐
│ DatabaseManager   │
│  (PostgreSQL)     │
└───────────────────┘
```

## 🔑 핵심 개념

### 1. 세션 관리
- **SessionManager**: 세션별 파일 저장/로드
- **PdfRegistry**: 분석 대기열 관리 (pdf_registry.json)
- **PageStorage**: 페이지별 결과 저장 (파일 시스템)

### 2. 상태 관리
- **session_state**: Streamlit 세션 상태
  - `uploaded_files_info`: 업로드된 파일 목록
  - `uploaded_file_objects`: 파일 바이너리 저장
  - `analysis_status`: 분석 상태
  - `review_data`: 검토 데이터

### 3. 데이터 저장
- **PostgreSQL**: 최종 파싱 결과 (parsing_sessions + items)
- **파일 시스템**: 임시 이미지, PDF 파일, 썸네일
- **JSON 파일**: 레거시 결과 (result/)

### 4. 멀티스레딩
- **ThreadPoolExecutor**: 최대 5개 스레드로 페이지 병렬 파싱
- 각 스레드는 독립적인 GeminiVisionParser 인스턴스 사용

## 📊 주요 함수 호출 체인

### 분석 실행
```
app.py::process_pdf_with_progress()
  └─→ PdfProcessor.process_uploaded_pdf()
      └─→ PdfProcessor.process_pdf()
          └─→ VisionParser.parse_pdf()
              └─→ GeminiVisionParser.parse_image() [멀티스레딩]
                  └─→ DatabaseManager.save_from_page_results()
```

### 검토 탭
```
app.py::render_review_tab()
  └─→ review_components.load_page_data()
      └─→ SessionManager.load_ocr_result()
          └─→ DatabaseManager.get_page_result() [우선]
              └─→ PageStorage.load_page() [폴백]
```

## 🎯 설계 원칙

1. **모듈화**: 기능별로 명확히 분리
2. **단일 책임**: 각 클래스는 하나의 책임만
3. **의존성 주입**: 외부 의존성 최소화
4. **폴백 메커니즘**: DB 실패 시 파일 시스템 사용
5. **상태 관리**: Streamlit rerun 구조에 맞춘 설계

